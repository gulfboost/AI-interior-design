{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.11",
    "uv": "latest"
  },
  "steps": {
    "install": {
      "inputs": [
        "...",
        { "local": true, "include": ["."] }
      ],
      "commands": [
        "uv --directory api sync --no-dev"
      ]
    }
  },
  "deploy": {
    "startCommand": "cd api && .venv/bin/uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
