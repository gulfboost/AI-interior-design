{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "python": "3.11"
  },
  "steps": {
    "install": {
      "commands": [
        { "cmd": "pip install uv", "customName": "Install uv" },
        { "cmd": "uv sync --no-dev --frozen", "customName": "Install Python dependencies" }
      ]
    }
  },
  "deploy": {
    "startCommand": ".venv/bin/uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
  }
}
