{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "22"
  },
  "steps": {
    "build": {
      "inputs": [
        "...",
        { "local": true, "include": ["frontend"] }
      ],
      "commands": [
        "npm install -g pnpm",
        "pnpm --dir frontend install",
        "pnpm --dir frontend run build",
        "cp -r /app/frontend/.next/static /app/frontend/.next/standalone/.next/static",
        "cp -r /app/frontend/public /app/frontend/.next/standalone/public",
        "cp $(which node) /app/frontend/.next/standalone/.node && chmod 755 /app/frontend/.next/standalone/.node"
      ],
      "deployOutputs": [
        { "include": ["frontend/.next/standalone"] }
      ]
    }
  },
  "deploy": {
    "startCommand": "PORT=${PORT:-3000} HOSTNAME=0.0.0.0 /app/frontend/.next/standalone/.node /app/frontend/.next/standalone/server.js"
  }
}
