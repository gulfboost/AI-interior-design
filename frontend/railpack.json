{
  "$schema": "https://schema.railpack.com",
  "packages": {
    "node": "22"
  },
  "steps": {
    "build": {
      "inputs": [
        "...",
        { "local": true, "include": ["frontend"] }
      ],
      "commands": [
        "npm install -g pnpm",
        "pnpm --dir frontend install",
        "pnpm --dir frontend run build"
      ],
      "deployOutputs": [
        { "include": ["frontend"] }
      ]
    }
  },
  "deploy": {
    "startCommand": "cd /app/frontend && node node_modules/next/dist/bin/next.js start -p ${PORT:-3000}"
  }
}
